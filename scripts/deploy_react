#!/bin/bash

# change into home directory and run npm build command
# appspec should have moved files into /home/ec2-user/portfolio

cd /home/ec2-user/portfolio
npm run build

# copy build contents to www folder where nginx will look for it
mv build/*  /var/www/
